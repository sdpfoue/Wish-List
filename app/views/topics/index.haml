=render 'subnav'

-if @topics.count==0
  该标签下还没有话题，
=link_to '发起话题', new_topic_url

=render 'topics'
            
-content_for :rside do
  -if @tag
    -if @wishes.count==0
      还没有标记为
      %i=@tag
      的愿望
    -else
      -if @tag
        标记了
        %i=@tag
        的愿望
      -else
        最新愿望
      %ul
        -@wishes.each do |wish|
          %li
            =link_to wish.name,wish_url(wish)
        
        

      
